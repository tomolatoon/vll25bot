コード変更後の仕上げを行います。以下を**この順番で**実行し、失敗したら修正してから次へ進んでください。

1. `bun format` — コードフォーマット
2. `bun lint-fix` — Lint の自動修正
3. `bun test` — テストの実行

いずれかのステップが失敗した場合は、エラーを修正してから同じステップを再実行し、全ステップが成功するまで繰り返してください。
